class Solution:
    def fib(self, n: int) -> int:
        def recur(n, dp={}):
            if n <=1:
                return n
            if n in dp:
                return dp[n]
            ans = recur(n-1) + recur(n-2)
            dp[n] = ans
            return ans
        return recur(n)
